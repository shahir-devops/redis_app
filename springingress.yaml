apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: production
  namespace: prod
spec:
  rules:
  - host: shahirdevops.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: springsvc
            port:
              number: 8080
